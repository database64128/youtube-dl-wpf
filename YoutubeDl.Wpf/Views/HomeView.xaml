<reactiveui:ReactiveUserControl
    x:Class="YoutubeDl.Wpf.Views.HomeView"
    x:TypeArguments="viewmodels:HomeViewModel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:reactiveui="http://reactiveui.net"
    xmlns:viewmodels="clr-namespace:YoutubeDl.Wpf.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
    d:DesignHeight="720" d:DesignWidth="848">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition x:Name="configureDownloadRowDefinition" Height="Auto" MinHeight="80" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="8 0 8 8"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}">
            Start your download
        </TextBlock>

        <ScrollViewer Grid.Row="1"
                      MaxHeight="{Binding ElementName=configureDownloadRowDefinition, Path=MaxHeight, Mode=OneWay}"
                      materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True"
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="40" MaxHeight="40" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           Margin="8"
                           VerticalAlignment="Center"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}">
                    Video link
                </TextBlock>
                <Grid Grid.Row="0" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="linkTextBox"
                             Grid.Column="0"
                             Margin="4"
                             VerticalAlignment="Center"
                             materialDesign:HintAssist.Hint="e.g. https://www.youtube.com/watch?v=b2390GAm4d0"/>
                    <Button x:Name="downloadButton"
                            Grid.Column="1"
                            Margin="4"
                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    <Button x:Name="listFormatsButton"
                            Grid.Column="2"
                            Margin="4"
                            Style="{StaticResource MaterialDesignOutlinedSecondaryButton}"
                            Content="_List formats"/>
                    <Button x:Name="abortButton"
                            Grid.Column="3"
                            Margin="4"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="Red"
                            Content="_Abort"/>
                </Grid>

                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           Margin="8"
                           VerticalAlignment="Center"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}">
                    Options
                </TextBlock>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
                    <TextBlock VerticalAlignment="Center" Margin="4 4 8 4">Preset</TextBlock>
                    <ComboBox x:Name="presetComboBox"
                              DisplayMemberPath="Name"
                              StaysOpenOnEdit="True"
                              Margin="4"
                              VerticalAlignment="Center">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>
                    <Button x:Name="addPresetButton"
                            Margin="4 4 2 4"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            ToolTip="Add custom preset"
                            VerticalAlignment="Center"
                            Height="32" Width="32">
                        <materialDesign:PackIcon Kind="Add" Height="20" Width="20" />
                    </Button>
                    <Button x:Name="editPresetButton"
                            Margin="2 4 2 4"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            ToolTip="Edit custom preset"
                            VerticalAlignment="Center"
                            Height="32" Width="32">
                        <materialDesign:PackIcon Kind="Edit" Height="20" Width="20" />
                    </Button>
                    <Button x:Name="duplicatePresetButton"
                            Margin="2 4 2 4"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            ToolTip="Duplicate preset"
                            VerticalAlignment="Center"
                            Height="32" Width="32">
                        <materialDesign:PackIcon Kind="ContentDuplicate" Height="20" Width="20" />
                    </Button>
                    <Button x:Name="deletePresetButton"
                            Margin="2 4 2 4"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            ToolTip="Delete custom preset"
                            VerticalAlignment="Center"
                            Height="32" Width="32">
                        <materialDesign:PackIcon Kind="Delete" Height="20" Width="20" />
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1">
                    <TextBlock VerticalAlignment="Center" Margin="4">Subtitles</TextBlock>
                    <CheckBox x:Name="subtitlesDefaultCheckBox"
                              Margin="8 4 4 4"
                              Style="{StaticResource MaterialDesignFilterChipPrimaryOutlineCheckBox}"
                              Content="Default" />
                    <CheckBox x:Name="subtitlesAutoGeneratedCheckBox"
                              Margin="4 4 8 4"
                              Style="{StaticResource MaterialDesignFilterChipSecondaryOutlineCheckBox}"
                              Content="Auto-generated" />
                    <ComboBox x:Name="subLangsComboBox"
                              Margin="4"
                              Width="96"
                              IsEditable="True"
                              IsReadOnly="False"
                              StaysOpenOnEdit="True"
                              materialDesign:HintAssist.Hint="Languages"
                              materialDesign:HintAssist.IsFloating="True">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>
                </StackPanel>

                <WrapPanel Grid.Row="3" Grid.Column="1">
                    <StackPanel Orientation="Horizontal" Height="40" Margin="0 0 4 0">
                        <ToggleButton x:Name="metadataToggle" Margin="4" />
                        <TextBlock VerticalAlignment="Center" Margin="4">
                            Add metadata
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Height="40" Margin="0 0 4 0">
                        <ToggleButton x:Name="thumbnailToggle" Margin="4"/>
                        <TextBlock VerticalAlignment="Center" Margin="4">
                            Download thumbnail
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Height="40" Margin="0 0 4 0">
                        <ToggleButton x:Name="playlistToggle" Margin="4"/>
                        <TextBlock VerticalAlignment="Center" Margin="4">
                            Download playlist
                        </TextBlock>
                        <TextBox x:Name="playlistItemsTextBox"
                                 Margin="4"
                                 Width="96"
                                 VerticalAlignment="Center"
                                 materialDesign:HintAssist.Hint="Items (optional)"
                                 materialDesign:HintAssist.IsFloating="True" />
                    </StackPanel>

                    <StackPanel x:Name="cookiesBrowserStackPanel"
                                Orientation="Horizontal"
                                Height="40"
                                Margin="0 0 4 0">
                        <ToggleButton x:Name="cookiesBrowserToggle" Margin="4" />
                        <TextBlock VerticalAlignment="Center" Margin="4">
                            Cookies (browser)
                        </TextBlock>
                        <ComboBox x:Name="cookiesBrowserComboBox"
                                  Margin="4"
                                  Width="96"
                                  IsEditable="True"
                                  IsReadOnly="False"
                                  StaysOpenOnEdit="True">
                            <ComboBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel />
                                </ItemsPanelTemplate>
                            </ComboBox.ItemsPanel>
                        </ComboBox>
                    </StackPanel>
                </WrapPanel>

                <Grid Grid.Row="4" Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ToggleButton x:Name="cookiesFileToggle"
                                  Grid.Row="0"
                                  Grid.Column="0"
                                  Margin="4" />

                    <TextBlock Grid.Row="0"
                               Grid.Column="1"
                               Margin="4"
                               VerticalAlignment="Center">
                        Cookies (file)
                    </TextBlock>

                    <ComboBox x:Name="cookiesFileComboBox"
                              Grid.Row="0"
                              Grid.Column="2"
                              Margin="4"
                              IsEditable="True"
                              IsReadOnly="False"
                              StaysOpenOnEdit="True">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>

                    <Button x:Name="cookiesFileBrowseButton"
                            Grid.Row="0"
                            Grid.Column="3"
                            Margin="4"
                            Style="{StaticResource MaterialDesignFlatButton}">
                        Browse
                    </Button>

                    <Button x:Name="cookiesFileShowInFolderButton"
                            Grid.Row="0"
                            Grid.Column="4"
                            Margin="4"
                            Style="{StaticResource MaterialDesignFlatButton}">
                        Show in folder
                    </Button>

                    <ToggleButton x:Name="pathToggle"
                                  Grid.Row="1"
                                  Grid.Column="0"
                                  Margin="4"/>

                    <TextBlock Grid.Row="1"
                               Grid.Column="1"
                               Margin="4"
                               VerticalAlignment="Center">
                        Download path
                    </TextBlock>

                    <ComboBox x:Name="pathComboBox"
                              Grid.Row="1"
                              Grid.Column="2"
                              Margin="4"
                              IsEditable="True"
                              IsReadOnly="False"
                              StaysOpenOnEdit="True">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>

                    <Button x:Name="browseButton"
                            Grid.Row="1"
                            Grid.Column="3"
                            Margin="4"
                            Style="{StaticResource MaterialDesignFlatButton}">
                        Browse
                    </Button>

                    <Button x:Name="openFolderButton"
                            Grid.Row="1"
                            Grid.Column="4"
                            Margin="4"
                            Style="{StaticResource MaterialDesignFlatButton}">
                        Open folder
                    </Button>

                    <ToggleButton x:Name="outputTemplateToggle"
                                  Grid.Row="2"
                                  Grid.Column="0"
                                  Margin="4"/>

                    <TextBlock Grid.Row="2"
                               Grid.Column="1"
                               Margin="4"
                               VerticalAlignment="Center">
                        Output template
                    </TextBlock>

                    <ComboBox x:Name="outputTemplateComboBox"
                              Grid.Row="2"
                              Grid.Column="2"
                              Margin="4"
                              IsEditable="True"
                              IsReadOnly="False"
                              StaysOpenOnEdit="True"
                              VerticalAlignment="Center"
                              FontFamily="pack://application:,,,/Resources/RobotoMono/#Roboto Mono">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>

                    <Button x:Name="resetOutputTemplateButton"
                            Grid.Row="2"
                            Grid.Column="3"
                            Margin="4"
                            Style="{StaticResource MaterialDesignFlatButton}">
                        Reset
                    </Button>
                </Grid>

                <TextBlock Grid.Row="5"
                           Grid.Column="0"
                           Margin="8"
                           VerticalAlignment="Center"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}">
                    Arguments
                </TextBlock>
                <ItemsControl Grid.Row="5"
                              Grid.RowSpan="2"
                              Grid.Column="1"
                              x:Name="argumentsItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>
        </ScrollViewer>

        <GridSplitter Grid.Row="2"
                      Height="5"
                      HorizontalAlignment="Stretch" />

        <TextBlock Grid.Row="3"
                   Margin="8"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}">
            Logs
        </TextBlock>

        <TextBox x:Name="resultTextBox"
                 Grid.Row="4"
                 Margin="8"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 TextWrapping="NoWrap"
                 IsReadOnly="True"
                 VerticalContentAlignment="Top"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 materialDesign:HintAssist.Hint="Output from backend"
                 FontFamily="pack://application:,,,/Resources/RobotoMono/#Roboto Mono"/>
    </Grid>
</reactiveui:ReactiveUserControl>
